/**
 * Entry point kept for familiarity. The implementation lives in server/index.js
 * (used by `npm run server` on Render). Run either:
 *   node multiplayer-server.js
 *   npm run server
 */
require("./server/index.js");
